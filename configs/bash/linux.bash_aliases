# flatpak sandbox prevents pre-commit hooks from running correctly
# alias smerge="flatpak run com.sublimemerge.App"
# flatpak sandbox gets in the way too much
# alias codium="flatpak run com.vscodium.codium"